using UnityEngine;
using System.Collections;

public class PlayerAlertIcon : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy(gameObject,2.5f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
