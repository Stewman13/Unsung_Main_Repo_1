using UnityEngine;
using System.Collections;

public class SoundTrigger : MonoBehaviour {				/*Jarred to whoever doing the proximity: Perhaps we should not do this so that the player does not become a hero, we could just do a standing in the light or camer
															starts to alert enemies. 1 turn in camera 2 turns in light and you are spotted?????*/

    void OnTriggerEnter(Collider other)
    {
        /*if (other.tag == "Hero" || other.tag == enemy)
        { 
            //Proximity Alert = true
            //Spawn GUITexture "?" above their head
            //Activate enemy Pathfinding
        }*/
    }
}
